# ClipFlow · macOS 剪贴板历史工具 — 开发交接规格

> 视觉与交互基准：本目录下 `clipflow-app.html`（可直接在浏览器打开，是唯一的视觉真源）。
> 本文档是把该原型翻译成 macOS 原生实现所需的全部约定。原型中的数值均为最终值，不要重新调参。

---

## 一、范围

### 第一版做
1. 自动记录 `Cmd+C` 内容，支持纯文本、代码、图片、URL 四种呈现
2. 悬浮窗查看历史，时间倒序
3. 搜索、收藏、删除、分类（全部 / 文字 / 图片 / 链接 / 收藏）
4. 选中记录后写回系统剪贴板（**不是模拟粘贴**，见 §6.2）
5. 偏好设置（隐藏入口，`⌘,` 唤起）
6. 隐私：仅本机存储、排除指定 App、两步确认清空

### 第一版不做
云同步 · iPhone 同步 · 用户账户 · AI 功能 · 多人协作 · 菜单栏图标以外的任何常驻 UI

---

## 二、技术栈建议

| 关注点 | 建议实现 |
|---|---|
| 窗口 | `NSPanel`，`.nonactivatingPanel` + `.hudWindow`，`level = .floating`，`hidesOnDeactivate = true`，无标题栏 |
| 毛玻璃 | `NSVisualEffectView`，`material = .hudWindow`（或 `.popover`），`blendingMode = .behindWindow`，`state = .active` |
| UI | SwiftUI 承载内容，外层用 `NSHostingView` 嵌入面板；列表用 `List` + 自管选中态，**不要**用系统 `.focusable()` 默认高亮 |
| 全局热键 | `Carbon` 的 `RegisterEventHotKey`（`⌥Space`）或 `NSEvent.addGlobalMonitorForEvents`；需 辅助功能授权时给出引导 |
| 剪贴板监听 | `NSPasteboard.general.changeCount` 轮询，间隔 `0.4s`；无系统级变更通知，轮询是标准做法 |
| 来源 App | 变更发生时取 `NSWorkspace.shared.frontmostApplication`，记录 `bundleIdentifier` + `localizedName` + 图标 |
| 存储 | SQLite（GRDB 或 `SQLite.swift`），库文件放 `~/Library/Application Support/ClipFlow/`；图片二进制单独落盘，DB 只存路径 |
| 权限 | 沙盒可开；剪贴板读取不需额外授权，全局热键需要辅助功能权限 |

---

## 三、数据模型

```swift
struct ClipItem: Identifiable {
    let id: Int64
    var kind: Kind              // .text / .code / .image / .link
    var category: Category      // .text / .image / .link  —— 收藏是标记，不是分类
    var isFavorite: Bool
    var sourceAppName: String   // "Visual Studio Code"
    var sourceBundleID: String
    var copiedAt: Date
    var title: String           // 列表摘要，单行截断
    var fullText: String?       // 文本/代码/链接的完整内容
    var imagePath: URL?         // 图片落盘路径
    var imageAlt: String?       // 无障碍描述，图片必填
    var meta: [(String, String)] // 预览面板的键值行，见下
}
```

`meta` 按类型给不同键，原型中的取值如下（照抄，不要改字段名）：

- 文本 / 代码：`类型`（`纯文本` 或 `纯文本 · Swift`）、`字符数`、`行数`
- 链接：`类型`=`网址`、`域名`、`标题`（抓取 `<title>`，失败则省略该行）
- 图片：`类型`（`PNG 图片` / `JPEG 图片`）、`尺寸`（`2400 × 1500`）、`大小`（`1.8 MB`）

预览面板固定在 `meta` 之后追加两行：`来源` = App 名，`复制时间` = `今天 14:32` 格式（当天用「今天」，昨天用「昨天」，更早用 `M月d日`）。
列表行的时间用相对格式：`刚刚` / `N 分钟前` / `N 小时前` / `昨天` / `M月d日`。

### 写入规则
- 连续两次相同内容不重复记录（比对 hash）
- 历史上限 **150 条**，超出淘汰最旧的**非收藏**项；收藏项永不被淘汰
- 排除列表中的 App 处于前台时，跳过本次记录
- 类型判定顺序：图片 → URL（`NSURL` 可解析且 scheme 为 http/https）→ 代码（含 `{}`/`=>`/`func`/`const` 等特征且多行）→ 纯文本

---

## 四、设计令牌

全部颜色用 OKLCh 定义（Swift 侧可转 `Color(.displayP3)` 或直接用 `NSColor` 的等效值）。深色为默认主题，浅色按 §4.4。

### 4.1 颜色

| 令牌 | 值 | 用途 |
|---|---|---|
| `fg` | `oklch(97% 0.004 260)` | 主文字、行标题 |
| `fg-2` | `oklch(84% 0.008 260)` | 次级文字、预览正文、分组小标题 |
| `muted` | `oklch(72% 0.012 260)` | 元信息、图标、占位、说明文字 |
| `accent` | `oklch(74% 0.13 218)` | 强调色（青蓝）**每屏最多 2 处可见** |
| `danger` | `oklch(70% 0.17 25)` | 删除、清空 |
| `star` | `oklch(83% 0.15 85)` | 收藏星标（琥珀） |
| `glass` | `oklch(24% 0.022 265 / 0.58)` | 主面板底色（半透明，靠模糊叠出玻璃） |
| `glass-2` | `oklch(20% 0.02 265 / 0.42)` | 预览面板底色，比主面板略深 |
| `chip` | `oklch(100% 0 0 / 0.08)` | 按钮 / 快捷键胶囊底 |
| `chip-hi` | `oklch(100% 0 0 / 0.14)` | 上述的 hover 态 |
| `hair` | `oklch(100% 0 0 / 0.08)` | 0.5px 分隔线 |
| `hair-2` | `oklch(100% 0 0 / 0.045)` | 更弱的分隔线 |
| `sel` | `oklch(100% 0 0 / 0.13)` | 选中行底色 |
| `skel` / `skel-hi` | `/0.07` / `/0.13` | 骨架屏渐变两端 |

**强调色纪律**：原型里 `accent` 只出现在两处 —— 选中行左侧的 2.5px 竖条、焦点环。链接图标刻意**不**上色。实现时不要再加第三处。

### 4.2 字体

| 角色 | 栈 |
|---|---|
| 正文 | `-apple-system` / `SF Pro Text` / `PingFang SC` |
| 等宽 | `SF Mono` / `Menlo` — 用于代码预览、快捷键胶囊 |

字号（全部为最终值）：行标题 `13px`、元信息 `11.5px`、预览正文 `11.5px`、按钮 `12px`、分类标签 `12.5px`、快捷键胶囊 `11px`、分组小标题 `11px`。**最小字号 11px，不要更小。**

中文行高下限：正文 `1.7`–`1.8`，说明文字 `1.65`。中文文本**不加负字距**。大写英文微标签字距 `0.08em`。

### 4.3 尺寸与圆角

- 面板宽度 `780px`（`max-width`），列表区高度 `min(412px, 52vh)`，最小 `216px`
- 主圆角 `12px`，小圆角 `7px`
- 列表 / 预览分栏：`1fr` + `296px`
- 列表行内边距 `8px 9px`，行间距 `1px 7px`
- 缩略图 `48 × 30`（对应 `320 × 200` 的 16:10 裁切，`aspectFill`）
- 图标按钮命中区 **≥ 24 × 24**（排除列表的 `×`、删除按钮均已按此实现）
- 玻璃质感三件套：`blur(48px) saturate(190%)` + 顶部 1px 内高光 + 长投影。原生实现由 `NSVisualEffectView` 提供模糊，内高光和投影仍需手动加

### 4.4 浅色主题

`外观` 设置项为「跟随系统」。浅色下把 `glass` 系反相为 `oklch(96% 0.004 265 / 0.62)`，`fg` → `oklch(22% 0.01 265)`，`muted` → `oklch(48% 0.012 265)`，`hair` 改为 `oklch(0% 0 0 / 0.1)`，`accent` 压暗到 `oklch(56% 0.14 218)` 以维持 4.5:1。其余令牌语义不变。

---

## 五、界面结构

面板自上而下四段，无标题栏：

```
┌──────────────────────────────────────────────┐
│ 🔍  搜索剪贴板历史…                      ⌘F  │  56px 搜索区
├──────────────────────────────────────────────┤
│ 全部 8   文字 3   图片 3   链接 2   收藏 2   │  分类栏（数字为实时计数）
├───────────────────────────┬──────────────────┤
│ ▍[缩略图] 标题             │  [大图 / 全文]   │
│    来源 App · 5 分钟前 ★  │                  │
│   [图标]  标题             │  类型   PNG 图片 │  列表 1fr
│    来源 App · 12 分钟前    │  尺寸   2400×1500│  预览 296px
│   …                        │  来源   Figma    │
│                            │ ─────────────────│
│                            │ [复制到剪贴板 ↵] │
│                            │ [收藏] [🗑]      │
├───────────────────────────┴──────────────────┤
│ ↑↓导航 ↵复制 ⌘S收藏 ⌘⌫删除 ⌘,设置   8条·本地│  底部快捷键条
└──────────────────────────────────────────────┘
```

搜索区左侧新增一级模式切换 `历史 / 提示词`。提示词不是剪贴板内容分类，而是用户主动维护、长期保存的文本模板：

```
┌──────────────────────────────────────────────┐
│ [历史][提示词] 🔍 搜索提示词…            ⌘F │
├──────────────────────────────────────────────┤
│ 全部 4   收藏 1   写作 2   产品 1     ＋新建 │
├───────────────────────────┬──────────────────┤
│ ▍[✦] 文章润色             │ 文章润色         │
│    写作 · 2 个变量 · 18次 │ {{语气}}…       │
│   [✦] PRD 评审            │ 分组   写作      │
│    产品 · 3 个变量 · 12次 │ 变量   2 个      │
│                            │ 使用   18 次     │
│                            │ [填写并复制 ↵]  │
│                            │ [编辑][收藏][删] │
├───────────────────────────┴──────────────────┤
│ ↑↓导航 ↵使用 ⌘S收藏 ⌘N新建       4条提示词 │
└──────────────────────────────────────────────┘
```

- 剪贴板历史是自动采集的临时记录，最多 150 条；提示词是主动维护的长期资产，不参与历史数量上限和淘汰。
- 提示词与历史共用同一 SQLite 文件，但必须使用独立 `prompts` 表；清空历史只能删除 `clips` 表中的数据。
- 提示词分组为单层自由文本。分类栏固定包含“全部”和“收藏”，随后显示从提示词数据动态计算出的分组及计数。
- 新建或编辑提示词时，分组字段默认表现为选择器，自动列出去重后的已有分组；选择器末尾提供“＋ 新建分组…”，只有选择该项后才展开名称输入框，避免用户反复手动输入已有分组。
- 分组的二级选择面板不使用系统原生白色下拉框；必须沿用 Jaimo Clip 的玻璃背景、圆角、细边框、选中色与青色强调色，并具备悬停、当前项勾选、`Esc` 关闭和点击外部关闭行为，深浅外观自适应。
- 普通分组项左侧不显示菱形或装饰图标，文字直接左对齐；右侧使用低强调度拖拽手柄。用户可用鼠标拖拽调整已有分组顺序，拖放位置显示青色插入指示；顺序必须持久化，并同步应用到选择面板和顶部分组标签。键盘可用 `Option+↑/↓` 完成同等排序。
- 提示词分组栏右侧必须固定显示强调度适中的“＋ 新建”按钮；它不参与横向滚动，不能只依赖 `⌘N` 或空状态入口。
- 提示词正文用 `{{变量名}}` 定义变量。同名变量只出现一次填写项，但渲染时替换正文内的全部同名标记。
- 无变量提示词的主操作是“复制提示词”；有变量提示词的主操作是“填写并复制”，先打开变量填写层，再复制最终文本。
- 从历史保存为提示词时创建独立副本；之后删除原历史记录不能影响提示词。
- 提示词复制仍是写入剪贴板，不模拟粘贴，并复用自身写入忽略机制，不能污染剪贴板历史。

- 搜索框必须有关联 `<label>`（原型用视觉隐藏的 `.sr`）—— placeholder 不算标签
- 分类栏是 tablist，计数从数据实时算出，**不要硬编码**
- 列表是 listbox，行是 option；选中行有 `sel` 底色 + `accent` 左竖条
- 收藏项在元信息末尾追加琥珀星标，并带无障碍文字「已收藏」
- 底部快捷键条在窄宽度下按优先级逐级隐藏（见 §7）

### 偏好设置（`⌘,` 唤起的覆盖层）
- 通用：开机自动启动（开关）· 唤起快捷键（`⌥Space`）· 历史记录上限（150 条）· 外观（跟随系统）· 复制后关闭窗口（开关）
- 隐私：仅保存在本机（`已启用 · 无云同步`）· 排除的应用列表（每项可移除）· 清空所有历史记录（两步确认）
- `role="dialog"` 语义：焦点进入面板、Tab 循环锁在面板内、关闭时焦点归还原处

---

## 六、交互规格

### 6.1 键盘（全部为唯一操作路径，鼠标只是补充）

| 按键 | 行为 |
|---|---|
| `⌥Space` | 全局唤起 / 收起面板。唤起时焦点落在搜索框，选中项复位到第一条 |
| `↑` / `↓` | 在当前筛选结果内移动选中项，到头不循环；选中项滚动进可视区 |
| `↵` | 把选中项写入系统剪贴板 |
| `⌘F` | 焦点跳到搜索框并全选已有文字 |
| `⌘1` / `⌘2` | 切换剪贴板历史 / 提示词库；两种模式分别保留搜索内容与选中状态 |
| `⌘N` | 打开提示词新建层；当前位于历史模式时先切换到提示词模式 |
| `⌘E` | 在提示词模式编辑选中提示词 |
| `⌘S` | 切换选中项收藏状态 |
| `⌘⌫` | 删除选中项，删除后选中项落到下一条 |
| `⌘,` | 开 / 关偏好设置 |
| `Tab` / `⇧Tab` | 循环切换分类（焦点不在搜索框和分类栏时） |
| `←` / `→` / `Home` / `End` | 焦点在分类栏时，在分类间移动（roving tabindex） |
| `ESC` | 搜索框有内容 → 清空搜索；否则关闭面板。设置层打开时 → 关闭设置层 |

**加载中和错误态下屏蔽除 `⌘F` / `ESC` 之外的所有快捷键**，避免对空数据执行操作。焦点在按钮上时 `↵` 交给按钮自己处理，不要重复触发复制。

### 6.2 复制行为（关键：不是粘贴）

用户明确要求这里是「复制到剪贴板」，不是模拟 `⌘V` 粘贴。

```swift
// 文本 / 代码 / 链接
NSPasteboard.general.clearContents()
NSPasteboard.general.setString(item.fullText ?? item.title, forType: .string)

// 图片
NSPasteboard.general.clearContents()
NSPasteboard.general.writeObjects([NSImage(contentsOf: item.imagePath!)!])
```

写入后：
- 文本类 toast：`已复制到剪贴板 · N 个字符`
- 图片类 toast：`已复制图片到剪贴板 · 1.8 MB`
- 失败 toast：`复制失败，请检查剪贴板权限`
- 若「复制后关闭窗口」开启，toast 展示 1.5s 后收起面板

**重要**：写入自己触发的 `changeCount` 变化必须被忽略，否则每次复制都会把同一条内容重新记录一次，历史被自己污染。做法是写入前记下即将产生的 `changeCount`，轮询时跳过它。

### 6.2.1 提示词变量填写

提示词编辑器在正文变化时解析 `{{变量名}}`，为每个变量保存“默认值”和“是否必填”。使用提示词时：

1. 无变量：`↵` 立即复制原正文，记录 `lastUsedAt` 并将 `useCount` 加一。
2. 有变量：`↵` 打开变量填写层，以默认值初始化所有输入项。
3. 必填变量为空时禁用“复制最终文本”；非必填变量允许为空并在渲染时替换为空字符串。
4. 填写值只用于本次渲染，不能覆盖提示词模板或变量默认值。
5. 复制成功后才更新使用次数；复制失败不能计数。

### 6.3 状态机

列表区有四种互斥状态，实现时按 `phase` 单一变量驱动：

| 状态 | 列表区 | 预览区 | 底部计数 |
|---|---|---|---|
| **加载中** | 5 行骨架屏（缩略图块 + 两条文字条，1.4s shimmer） | `正在读取本地历史…` | `加载中…` |
| **错误** | 警告图标 + `无法读取剪贴板历史` + 说明 + `重试` 按钮 | `读取失败，暂无预览` | `读取失败` |
| **空** | 剪贴板图标 + 分场景文案（见下） | `选中一条记录后 / 这里显示完整内容` | `0 条 · 本地存储` |
| **就绪** | 记录行 | 大图/全文 + 元信息 + 三个操作 | `N 条 · 本地存储` |

空态文案分三种，不要合成一句通用的：
- 搜索无结果：`没有匹配「关键词」的记录` / `试试更短的关键词，或按 ESC 清空搜索后切换其他分类。`
- 收藏为空：`还没有收藏的记录` / `选中一条记录后按 ⌘S 收藏，收藏项不会被数量上限淘汰。`
- 分类为空：`图片分类下暂无记录` / `复制任意内容后会自动出现在这里。按 ⌥Space 可随时唤起窗口。`

错误态说明文字：`本地数据库可能被占用或损坏。重试仍失败时，可在偏好设置中清空历史重建。`

### 6.4 两步确认清空

清空历史不能一击生效：

1. 首次点击 → 按钮变红（`danger` 底 + 边框），文字变 `确认清空？再次点击执行`，说明文字变 `将永久删除全部 N 条记录（含收藏）。8 秒内未确认将自动取消。`
2. 8 秒内再次点击 → 执行，关闭设置层，toast `已清空 N 条历史记录`
3. 8 秒未确认或关闭设置层 → 自动复位

### 6.5 交互态配色（逐个核对，不要只做 hover）

| 元素 | default | hover | active | focus | disabled |
|---|---|---|---|---|---|
| 列表行 | 透明 | `chip` | `/0.1` | 选中行 `sel` + `accent` 左竖条 | — |
| 普通按钮 | `chip` | `chip-hi` | `/0.2` | 2px `accent` 环 | `opacity .42`，不可点 |
| 删除按钮 | `chip` + `fg-2` 文字 | `danger/0.16` 底 + `oklch(84% .11 25)` 文字 | — | 同上 | — |
| 移除 `×` | `muted` | 同删除按钮配色 | — | 2px `accent` 环，`offset 1px` | — |
| 搜索区 | 透明 | — | — | `focus-within` 时底色 `/0.04` | — |

铁律：hover 时前景色**绝不**变灰或靠近背景，对比度只能升不能降。正文 ≥ 4.5:1，图标和大字 ≥ 3:1。只有 disabled 允许降对比。

---

## 七、响应式

面板是固定宽度的悬浮窗，但内容区需要在窄宽度下退化（用户缩小窗口或小屏 Mac）：

- **≤ 720px**：预览面板从右侧列改为列表下方一行（`grid-row: 2`），隐藏大图和元信息区，**保留三个操作按钮** —— 否则窄屏下没有任何复制入口。列表高度降到 `min(360px, 42vh)`。快捷键条只留前 3 项。分类栏改为横向可滚动（滚动条隐藏）。
- **≤ 520px**：快捷键条只留前 2 项，搜索区内边距收到 `13px`，`复制到剪贴板` 按钮占 2 份宽度。
- 面板外边距用 `clamp(16px, 5vh, 56px)`，短视口不裁切窗口。

原生实现对应：设面板最小宽度 `520pt`，在 `520–720pt` 区间走单列布局。

---

## 八、无障碍（WCAG 2.2 AA）

原型已按下列要求实现，原生侧需用 `NSAccessibility` 对应：

1. 搜索框：`combobox` 角色，`aria-controls` 指向列表，有关联标签
2. 列表：`listbox` + `option`，选中项通过 `aria-activedescendant` 播报（原生用 `accessibilitySelectedChildren`）
3. 分类栏：`tablist` + `tab`，`aria-selected`，roving tabindex（只有选中项 `tabIndex = 0`）
4. 图片：必须有 `imageAlt` 文字描述，不能只有文件名
5. toast：`role="status" aria-live="polite"`，屏幕阅读器能听到复制结果
6. 错误态：`role="alert"`
7. 加载中：`aria-busy="true"`
8. 所有可聚焦元素有可见焦点环，**不允许移除**
9. 命中区 ≥ 24 × 24（触控目标标准；若未来上 iPad 需提到 44 × 44）
10. `prefers-reduced-motion` 下关闭所有动效，shimmer 退化为静态底色，弹出动画退化为纯不透明度渐变。**动效永远不是状态的唯一信号。**

---

## 九、验收清单

- [ ] `⌥Space` 在任意 App 前台都能唤起，面板不抢走原 App 的焦点
- [ ] 复制自己的写入不会产生新历史条目（§6.2）
- [ ] 分类计数与实际数据一致，删除/收藏后立即更新
- [ ] 排除列表中的 App（1Password、钥匙串访问、终端）复制内容时不记录
- [ ] 150 条上限触发时淘汰最旧的非收藏项，收藏项保留
- [ ] 四种状态（加载/错误/空/就绪）都能实际触发并正确渲染
- [ ] 空态在三种场景下给出三种不同文案
- [ ] 窄宽度下复制按钮仍然可达
- [ ] 全键盘走通一遍：唤起 → 搜索 → 切分类 → 选中 → 复制 → 关闭，全程不碰鼠标
- [ ] VoiceOver 走通一遍，选中项变化、复制结果、错误都有播报
- [ ] 关闭系统「减弱动态效果」后再开启，动效正确降级
- [ ] 浅色 / 深色主题切换后对比度均达标

---

## 十、已知未定项

以下项原型中给了合理默认值，但需要产品确认：

1. **快捷键冲突**：`⌥Space` 与部分输入法切换冲突，是否需要在设置里做成可自定义（原型里是只读展示）
2. **图片存储上限**：150 条里若全是大图，磁盘占用仍可能到 GB 级。是否需要独立的图片数量或总体积上限
3. **代码语言识别**：原型的 `纯文本 · Swift` 暗示做了语言判定。是否引入轻量识别库，还是统一显示 `纯文本`
4. **链接标题抓取**：需要联网请求目标页取 `<title>`，与「仅本机存储」的隐私承诺存在张力。建议默认关闭，设置里提供开关
