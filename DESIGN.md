---
version: alpha
name: Jaimo Island
description: macOS 本地个人工具站的灵动岛式界面设计语言
colors:
  foreground: "oklch(97% 0.004 260)"
  foreground-secondary: "oklch(84% 0.008 260)"
  muted: "oklch(72% 0.012 260)"
  accent: "oklch(74% 0.13 218)"
  danger: "oklch(70% 0.17 25)"
  favorite: "oklch(83% 0.15 85)"
  glass: "oklch(24% 0.022 265 / 0.88)"
  glass-secondary: "oklch(20% 0.02 265 / 0.72)"
  control: "oklch(100% 0 0 / 0.08)"
  control-hover: "oklch(100% 0 0 / 0.14)"
  border: "oklch(100% 0 0 / 0.10)"
  selection: "oklch(100% 0 0 / 0.13)"
typography:
  body:
    fontFamily: "-apple-system, BlinkMacSystemFont, SF Pro Text, PingFang SC, system-ui, sans-serif"
  mono:
    fontFamily: "SF Mono, Menlo, ui-monospace, monospace"
---

## Overview

Jaimo Island 是只运行于 macOS 的本地个人工具站。它通过屏幕顶部居中的完整工具站统一提供首页、应用、提示词和剪切板能力。界面延续现有 Jaimo clip 的本地优先、高密度和原生玻璃质感，并将导航与各项能力组织为可扩展的模块。

## Colors

强调色只用于当前导航、当前选中项或主操作中最需要注意的一个层级。同一视图最多出现两处高饱和强调。收藏和危险操作分别使用独立的黄色与红色语义，不得代替当前导航的青色强调。

## Themes

| Role | Dark | Light |
| --- | --- | --- |
| foreground | `oklch(97% 0.004 260)` | `oklch(22% 0.01 265)` |
| foreground-secondary | `oklch(84% 0.008 260)` | `oklch(34% 0.01 265)` |
| muted | `oklch(72% 0.012 260)` | `oklch(48% 0.012 265)` |
| accent | `oklch(74% 0.13 218)` | `oklch(56% 0.14 218)` |
| glass | `oklch(24% 0.022 265 / 0.88)` | `oklch(96% 0.004 265 / 0.94)` |
| glass-secondary | `oklch(20% 0.02 265 / 0.72)` | `oklch(91% 0.006 265 / 0.88)` |
| control | `oklch(100% 0 0 / 0.08)` | `oklch(0% 0 0 / 0.07)` |
| control-hover | `oklch(100% 0 0 / 0.14)` | `oklch(0% 0 0 / 0.12)` |
| border | `oklch(100% 0 0 / 0.10)` | `oklch(0% 0 0 / 0.10)` |
| selection | `oklch(100% 0 0 / 0.13)` | `oklch(0% 0 0 / 0.09)` |

## Typography

所有主要界面使用 macOS 系统字体栈，以保持与原生应用一致的中英文排版。代码、快捷键和具有固定宽度语义的数据使用等宽字体。用户界面最小字号不低于 11pt；时间、数量和统计数据使用等宽数字。

## Layout

应用具有隐藏、紧凑灵动岛和完整工具站三种顶层窗口状态。全局快捷键 `⌥Space` 从隐藏态唤起紧凑灵动岛；在紧凑态或完整工具站再次触发快捷键会直接隐藏。菜单栏左键和菜单中的“显示 Jaimo 工具站”从隐藏态直接打开完整工具站。

紧凑灵动岛显示时间、本地运行状态、快速便签、摄像头检查和展开操作。点击主体或展开操作进入完整工具站；点击外部或按 `Escape` 返回隐藏态。完整工具站按 `Escape` 时先关闭模态和页面二级状态，再收起为紧凑灵动岛；点击窗口外部也收起为紧凑灵动岛。右上角隐藏操作直接返回隐藏态。三种状态都保持在鼠标所在屏幕顶部居中。

完整工具站的一级导航位于顶部，顺序固定为首页、应用、提示词和剪切板，设置作为右侧全局操作。一级导航不与页面内的分类筛选器混用。

首页使用有限网格组织时间、便签、摄像头检查和最近应用。组件允许排序和隐藏，但不允许任意尺寸拉伸。应用页使用左侧常用区和右侧全部应用区的两栏结构。提示词与剪切板保留列表和详情预览的高密度双栏结构。

## Elevation & Depth

窗口背景使用 macOS 原生材质和半透明色层构成景深。只有顶层工具站、模态层和当前拖拽对象使用显著投影；页面内容优先使用表面明度和边框区分层级，避免每个组件都变成浮起卡片。背景模糊表面在状态切换中不参与动画。

## Shapes

完整工具站使用连续大圆角矩形。内部列表项、输入框和按钮使用较小的同系圆角，不将每个标签和非交互文本放入胶囊。

## Components

完整导航使用图标和文字共同表达页面，当前页需要同时具有颜色与形状信号。窗口右上角同时提供“收起为灵动岛”和“隐藏 Jaimo”两个含义明确的操作，二者必须具有不同的图标、无障碍名称和帮助文本。

首页组件共用统一的标题、辅助说明和工具操作区，组件编辑状态提供排序与隐藏操作。空状态必须提供一个与当前任务直接相关的主操作。

应用页的常用项来自用户收藏，全部应用来自 `/Applications`、`/System/Applications` 和 `~/Applications` 的本地扫描结果。启动、收藏和搜索在同一页内完成。

提示词和剪切板沿用现有的搜索、分类、列表、预览和主操作语义。二者是独立页面，不再在搜索栏中使用“历史 / 提示词”二级模式切换。

## Do's and Don'ts

- 所有图标按钮必须具有可读取名称，所有可聚焦元素必须保留可见焦点样式。
- 所有可操作命中区不小于 24pt；摄像头、数据删除和权限失败在操作发生的位置就近显示反馈。
- 入场和状态反馈只使用位移和不透明度，交互反馈不超过 200ms，并尊重系统“减弱动态效果”。
- 不用动画作为状态的唯一信号，不在切换时动画大面积模糊或背景材质。
- 不使用模拟粘贴代替剪切板写入，不将提示词使用记录与剪切板历史混存。
- 不将云同步、账户、Windows 兼容或第三方插件市场纳入第一版。
